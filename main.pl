#!/usr/bin/perl

require "./library.pl";

# parse command-line arguments, such as config file path

# just need to do this once(?)
# read system default, then the instance's own adjustments
read_configuration_file(); 
